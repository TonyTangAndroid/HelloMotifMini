plugins {
    id 'java-library'
}
java {
    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8
}

def motifVersion = '0.4.3-LOCAL'
dependencies {
    //Local maven setup
    implementation "com.uber.motif:lib:${motifVersion}"
    annotationProcessor "com.uber.motif:compiler:${motifVersion}"

    testImplementation 'junit:junit:4.13.2'
    testImplementation 'com.google.truth:truth:1.1.3'
}
